# Web server
Flask==3.1.3
Flask-Limiter==4.1.1

# Auth tokens (PyJWT provides the `jwt` module used in main.py).
# NOTE: do NOT `pip install jwt` -- that is a different, incompatible package.
PyJWT==2.14.0

# Config file parsing (https://github.com/olliez-mods/Quick-Ini)
quick_ini==3.2
